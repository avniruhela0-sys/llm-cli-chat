# LLM CLI Chat

A simple command-line chat tool that uses **Google Gemini** to answer your questions.

## Features
- Interactive chat loop
- Stores your API key securely via `.env`
- Uses Gemini's free tier (no credit card required)

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/avniruhela0-sys/llm-cli-chat.git
   cd llm-cli-chat